import React, { useContext } from 'react';
import ShoeContext from '../Context/ShoeContext';

const Cart = () => {
    // const { count, Large, Medium, Small, Price, price } = useContext(ShoeContext);

    return (
        <div>
            {/* <p>Cart: {count}</p>
            <p>Large: {Large.quantity}</p>
            <p>Medium: {Medium.quantity}</p>
            <p>Small: {Small.quantity}</p>
            <p>Price: {Price}</p>
            <div>
                <p>Total Price: {price}</p>
            </div> */}
        </div>
    );
};

export default Cart;
